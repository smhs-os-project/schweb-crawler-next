/**
 * 頁面類型。
 */
export enum PageType {
    /**
     * 首頁。
     */
    Root = "root",
    /**
     * 公告頁。
     */
    Announcement = "announcement",
    /**
     * 檔案（TODO）。
     */
    Files = "files",
}
