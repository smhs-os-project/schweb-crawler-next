{
    "title": "The SMHS crawler configuration.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "smhsUrl": {
            "title": "SMHS reverse proxy URL",
            "type": "string",
            "description": "The URL to SMHS reverse proxy. See `reverse_proxy` folder.",
            "editor": "textfield"
        }
    },
    "required": [
        "smhsUrl"
    ]
}
